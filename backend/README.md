# ResumeAI Backend

This directory contains the Node.js/Express backend API for the ResumeAI platform.

## 🚀 Quick Start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Configure environment variables in `.env`:
   ```env
   PORT=5000
   MONGODB_URI=your_mongodb_connection_string
   JWT_SECRET=your_jwt_secret
   OPENAI_API_KEY=your_openai_key
   GEMINI_API_KEY=your_gemini_key
   CLIENT_URL=http://localhost:3000
   ```

3. Start the server:
   ```bash
   npm start
   ```

---

**For full project documentation, setup instructions, and features, please refer to the [Main README](../README.md).**
